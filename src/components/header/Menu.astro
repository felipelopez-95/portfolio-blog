---

---

<button id="menu">
    <div></div>
    <div></div>
    <div></div>
</button>

<style>
    button {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        width: 32px;
        height: 24px;
        border: 0;
        background: transparent;
        gap: 6px;
        padding: 0;
        cursor: pointer;
    }

    button > div {
        background: white;
        width: 100%;
        height: 4px;
        border-radius: 4px;
        transition:
            transform 0.4s cubic-bezier(0.4, 0, 0.2, 1),
            opacity 0.3s ease;
        transform-origin: center;
    }

    button.is-active div:first-child {
        transform: translateY(10px) rotate(45deg);
    }

    button.is-active div:nth-child(2) {
        opacity: 0;
    }

    button.is-active div:last-child {
        transform: translateY(-10px) rotate(-45deg);
    }
</style>
