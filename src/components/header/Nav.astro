---

---

<nav class="nav">
    <ul id="nav" class="nav-list">
        <li class="nav-item">
            <a href="#" class="nav-link">Expriencia</a>
        </li>
        <li class="nav-item">
            <a href="#" class="nav-link">Proyectos</a>
        </li>
        <li class="nav-item">
            <a href="#" class="nav-link">Sobre mí</a>
        </li>
        <li class="nav-item">
            <a href="#" class="nav-link">Blog</a>
        </li>
    </ul>
</nav>

<style>
    .nav-list {
        display: flex;
        flex-direction: column;
        align-items: flex-end;
        gap: 0.5rem;
        padding: 0.7rem 1rem;
        margin: 0;
        list-style: none;
    }

    .nav-item {
        background: var(--bg-secondary, #1a1a1a);
        border: 1px solid rgba(255, 255, 255, 0.03);
        border-radius: 10px;
        box-shadow:
            0 4px 6px -1px rgba(0, 0, 0, 0.5),
            0 10px 15px -3px rgba(0, 0, 0, 0.4);
    }

    .nav-item:hover {
        background: var(--bg-elevated, #252525);
        box-shadow: 0 15px 25px -5px rgba(0, 0, 0, 0.6);
        border-color: rgba(255, 255, 255, 0.1);
    }

    .nav-link {
        display: block;
        padding: 0.6rem 1.5rem;
        text-decoration: none;
        font-weight: 500;
        font-size: 1rem;
        color: var(--text-primary);
    }

    @media (min-width: 768px) {
        .nav-list {
            flex-direction: row;
            gap: 1rem;
            padding: 0.3rem 1rem;
        }

        .nav-item {
            background: transparent;
            border: none;
            box-shadow: none;
            border-radius: 0;
            border-bottom: 2px solid transparent; /* Borde invisible para reservar el espacio */
            transition: border-color 0.3s ease;
        }

        .nav-item:hover {
            border-color: var(--accent-primary);
        }

        .nav-link {
            padding: 0;
            font-size: 0.85rem;
        }
    }
</style>
