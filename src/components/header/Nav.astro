---

---

<nav class="nav">
    <ul id="nav" class="nav-list">
        <li class="nav-item">
            <a href="#experience" class="nav-link">Expriencia</a>
        </li>
        <li class="nav-item">
            <a href="#projects" class="nav-link">Proyectos</a>
        </li>
        <li class="nav-item">
            <a href="#about" class="nav-link">Sobre mí</a>
        </li>
    </ul>
</nav>

<style>
    .nav-list {
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
        padding: 0.7rem 1rem;
        margin: 0;
        list-style: none;
        width: 100%;
    }

    .nav-item {
        background: var(--bg-secondary, #1a1a1a);
        border: 1px solid rgba(255, 255, 255, 0.03);
        border-radius: calc(infinity * 1px);
        box-shadow: var(--box-shadow);
    }

    .nav-link {
        display: block;
        padding: 0.6rem 1.5rem;
        text-decoration: none;
        font-weight: 500;
        font-size: 1rem;
        color: var(--text-primary);
        text-align: center; /* Centra el texto para que todos se vean uniformes */
    }

    @media (min-width: 768px) {
        .nav-list {
            flex-direction: row;
            align-items: center; /* Alineación horizontal en desktop */
            gap: 1rem;
            padding: 0.3rem 1rem;
            width: auto;
        }

        .nav-item {
            background: transparent;
            border: none;
            box-shadow: none;
            border-radius: 0;
            border-bottom: 2px solid transparent;
            transition: border-color 0.3s ease;
        }

        .nav-item:hover {
            border-color: var(--accent-primary);
        }

        .nav-link {
            padding: 0;
            font-size: 0.85rem;
            text-align: left; /* Reset del centrado para desktop */
        }
    }
</style>
